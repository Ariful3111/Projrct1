import 'package:flutter/material.dart';
class RecordAnswer extends StatelessWidget {
  const RecordAnswer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(child: Text('Do something')),
    );
  }
}
